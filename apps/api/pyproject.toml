[project]
name = "api"
version = "0.1.0"
description = "SkillPolaris HTTP API"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.121.0",
    "psycopg2-binary>=2.9.11",
    "pydantic-ai>=2.8.0",
    "pydantic-settings>=2.13.1",
    "pypdf>=6.0.0",
    "python-multipart>=0.0.20",
    "sqlalchemy>=2.0.0",
    "uvicorn[standard]>=0.38.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/api"]

[tool.hatch.build.targets.wheel.force-include]
"src/api/prompts/cv_extract.md" = "api/prompts/cv_extract.md"
